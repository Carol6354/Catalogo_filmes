# PostgreSQL. Versions 9.3 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   gem install pg -- --with-pg-config=/usr/local/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem "pg"
#
default: &default
  adapter: postgresql
  encoding: unicode
  pool: 5
  host: localhost
  username: movie_catalog
  password: "122520Am"

development:
  <<: *default
  database: catalogo_filmes_development

test:
  <<: *default
  database: catalogo_filmes_test

production:
  primary: &primary_production
    <<: *default
    database: catalogo_filmes_production
    username: catalogo_filmes
    password: <%= ENV["CATALOGO_FILMES_DATABASE_PASSWORD"] %>
  cache:
    <<: *primary_production
    database: catalogo_filmes_production_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    database: catalogo_filmes_production_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_production
    database: catalogo_filmes_production_cable
    migrations_paths: db/cable_migrate
